<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ja.finalproject.user.mapper.UserSqlMapper">
    <!-- 회원 가입 -->
    <insert id="createUser">
        INSERT INTO fp_user (id, user_id, password, nickname, email, gender, birth, phone)
        VALUES (#{id}, #{userId},#{password},#{nickname},#{email},#{gender},#{birth},#{phone})
    </insert>

    <select id="loginCheck" parameterType="com.ja.finalproject.dto.UserDto" resultType="int">
      SELECT  COUNT(*) 
      FROM    fp_user A
      WHERE   A.user_id = #{userId}
      AND     A.password = #{password}
    </select>
    
</mapper>